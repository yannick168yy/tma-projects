<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              OAuth 回跳拦截：Google/Telegram 授权在 Custom Tab 里完成后会回跳我们自己的
              /auth/xxx/callback，系统据此把 URL 交回 App，登录态才落在 App 的 WebView 里。
              autoVerify 依赖服务器上的 /.well-known/assetlinks.json（见 android-shell/README.md）。
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- 只注册规范域：betogo.games 裸域 301 到 www，而 App Link 验证不跟随重定向，
                     注册了也永远验不过；betogo.app 则相反，裸域直出（www 未配证书），故注册裸域。
                     OAuth 回跳走 location.origin，App 跑在哪个域回跳就是哪个域 -->
                <data android:scheme="https" android:host="betogo.app" android:pathPrefix="/auth/" />
                <data android:scheme="https" android:host="www.betogo.games" android:pathPrefix="/auth/" />
                <data android:scheme="https" android:host="www.188facai.com" android:pathPrefix="/auth/" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- KYC 证件上传要拍照/选图 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
</manifest>
