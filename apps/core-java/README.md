# core-java

Spring Boot 3.x：聚合商竞彩回调、MQ 账变、`MessageQueueService` 抽象、钱包领域。

- 包结构按 `wallet` / `callback` / `consumer` / `integration` / `mq` 分治。
- 目录约定见 [docs/STRUCTURE.md](../../docs/STRUCTURE.md)。

下一步：`spring init` 或 start.spring.io 生成工程，拷贝包路径骨架。
