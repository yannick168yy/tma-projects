# Java Core placeholder — Spring Boot 项目待定
# 启用: docker compose --profile java up -d

FROM eclipse-temurin:21-jre-alpine
WORKDIR /app

RUN apk add --no-cache wget

EXPOSE 8080

# 占位进程；实现 core-java 后替换为 Spring Boot JAR
CMD ["sh", "-c", "echo 'core-java not implemented — enable after Spring Boot scaffold' && sleep infinity"]
