---
description: After code changes, auto commit and deploy to Alibaba Cloud (no push)
alwaysApply: true
---

# Git & Deploy：改完代码自动 commit + 部署到阿里云（不需要 push）

**每次代码改完后，必须自动执行以下流程（无需用户重复要求）：**

1. `git add` 相关文件
2. `git commit`（使用 `/usr/local/bin/git`，避免系统自带 2.24.3 的兼容问题）
3. **不需要** `git push`
4. 根据改动范围选择部署方式（见下方）

---

## 部署方式选择

### 纯前端改动（`apps/web-tma/` 下的文件）→ 快速部署

```bash
cd /Users/yannicky/tma-projects && \
DEPLOY_HOST=root@47.84.34.139 \
DEPLOY_DIR=/root/workspace/tma-projects \
SSH_IDENTITY_FILE=~/Downloads/yannick.pem \
SSH_OPTS="-o StrictHostKeyChecking=no" \
bash deploy/single-node/deploy-web-tma-fast.sh
```

原理：本地 `npm run build` → rsync `dist/` 到服务器 → nginx 即时生效，无需重建容器。

### 后端有改动（`apps/bff-node/`、`apps/core-node/`、`deploy/`、`.env` 等）→ 完整部署

```bash
cd /Users/yannicky/tma-projects && \
DEPLOY_HOST=root@47.84.34.139 \
DEPLOY_DIR=/root/workspace/tma-projects \
SSH_IDENTITY_FILE=~/Downloads/yannick.pem \
SSH_OPTS="-o StrictHostKeyChecking=no" \
bash deploy/single-node/deploy-web-tma.sh
```

---

## 注意事项

- git 命令用 `export PATH="/usr/local/bin:$PATH"` 或直接用 `/usr/local/bin/git`
- commit message 用 `-m` 单行，或写入临时文件后用 `-F` 传入
- **不需要**用户说"部署"才执行，改完代码即自动完成全流程
- 服务器地址：`47.84.34.139`，域名：`https://www.188facai.com`

## 例外（仍需用户明确要求）

- 创建 PR / push 到远程仓库
- 数据库 DDL 变更上生产前需确认
- 涉及密钥/环境变量变更
