---
description: 代码改完后自动 commit + 部署测试环境；push 等用户通知
alwaysApply: true
---

# Git & Deploy：改完自动 commit + 部署（push 等通知）

**本项目默认流程：完成代码修改后，任务结束前自动执行以下步骤，无需用户再提醒。**

1. `git add` 本次相关文件（不含 `.env` 等密钥）
2. `git commit`（中文 message，说明 why）
3. **Fast 部署阿里云测试环境**（见下方；纯代码改动的默认方式）
4. **`git push` 不要自动执行** — 等用户明确说「push」「推上去」等再执行

## 部署命令（默认：Fast，约 20–40 秒）

按改动选择目标（可多选，空格分隔；省略参数等价于 `all`）：

```bash
cd /Users/yannicky/tma-projects && \
DEPLOY_HOST=root@47.84.34.139 \
DEPLOY_DIR=/root/workspace/tma-projects \
SSH_IDENTITY_FILE=/Volumes/MacAPFS/TMA_FILES/亚马逊云-阿里云/aliyun.pem \
SSH_OPTS="-o StrictHostKeyChecking=no" \
bash deploy/single-node/deploy-fast.sh web-tma
# 或: bff-node | core-node | all
```

| 改动范围 | 目标 |
|----------|------|
| 仅 `apps/web-tma` | `web-tma` |
| 仅 `apps/bff-node` | `bff-node` |
| 仅 `apps/core-node` | `core-node` |
| 前后端都有 | `all` |

## 完整部署（仅必要时，约 3–5 分钟）

**新 npm 依赖 / Dockerfile / 首次部署 / 数据库迁移 / compose 变更** 时才用：

```bash
cd /Users/yannicky/tma-projects && \
DEPLOY_HOST=root@47.84.34.139 \
DEPLOY_DIR=/root/workspace/tma-projects \
SSH_IDENTITY_FILE=/Volumes/MacAPFS/TMA_FILES/亚马逊云-阿里云/aliyun.pem \
SSH_OPTS="-o StrictHostKeyChecking=no" \
bash deploy/single-node/deploy-web-tma.sh
```

完整部署后建议 SSH 执行 `podman image prune -f` 清理悬空镜像。

## 注意事项

- 服务器：`47.84.34.139`，项目路径：`/root/workspace/tma-projects`
- 容器通过 volume 挂载 `apps/*/dist`，Fast 部署无需重建镜像
- 不提交 `.env`、密钥、pem 等敏感文件
- 本地 Docker（`./scripts/local-deploy.sh`）仅在需要本地验证时额外执行
- commit 优先用 `/usr/local/bin/git`（系统 git 可能与 hook 不兼容）

## 仍需用户明确要求

- **`git push`**（默认不做）
- 创建 PR
- 数据库 DDL 上生产前确认
- 涉及密钥/环境变量变更
- 用户明确说「先别提交 / 别部署」
