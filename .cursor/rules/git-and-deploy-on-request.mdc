---
description: After web-tma changes, auto commit + push + fast deploy
alwaysApply: true
---

# Git & Deploy：改完自动 commit + push + 部署

**web-tma 前端改完后自动执行（无需用户再说）：**

1. `git add` 相关文件
2. `git commit`（`/usr/local/bin/git`，中文 message）
3. `git push origin main`
4. 快速部署（仅前台）：

```bash
cd /Users/yannicky/tma-projects && \
DEPLOY_HOST=root@47.84.34.139 \
DEPLOY_DIR=/root/workspace/tma-projects \
SSH_IDENTITY_FILE=~/Downloads/yannick.pem \
SSH_OPTS="-o StrictHostKeyChecking=no" \
bash deploy/single-node/deploy-web-tma-fast.sh
```

**含 bff-node / core-node / deploy 等后端改动时**，改用完整部署 `deploy/single-node/deploy-web-tma.sh`。

## 注意事项

- 不提交 `.env` 等含密钥文件
- 服务器：`47.84.34.139`

## 仍需用户明确要求

- 创建 PR
- 数据库 DDL 上生产前确认
- 涉及密钥/环境变量变更
