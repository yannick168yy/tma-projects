---
description: After code changes, auto commit+push+deploy to Alibaba Cloud by default
alwaysApply: true
---

# Git & deploy: 默认自动发布到阿里云

**每次代码改完后，必须自动执行以下全流程（无需用户重复要求）：**

1. `git add` 相关文件
2. `git commit`（使用 `/usr/local/bin/git`，系统 git 版本过旧不支持 --trailer）
3. `git push origin main`
4. 部署到阿里云：
   ```bash
   DEPLOY_HOST=root@47.84.34.139 \
   DEPLOY_DIR=/root/workspace/tma-projects \
   SSH_IDENTITY_FILE=~/Downloads/yannick.pem \
   SSH_OPTS="-o StrictHostKeyChecking=no" \
   bash deploy/single-node/deploy-web-tma.sh
   ```

**注意事项：**
- git 命令必须用 `export PATH="/usr/local/bin:$PATH"` 或直接用 `/usr/local/bin/git`，避免系统自带 2.24.3 的 --trailer 兼容问题
- commit message 用 `-m` 单行或写入临时文件再用 `-F` 传入
- **不需要**用户说"部署"、"push"才执行，改完代码即自动完成全流程

**例外（仍需用户明确要求）：**
- 创建 PR
- 数据库 DDL 变更上生产前需确认
- 涉及密钥/环境变量变更
