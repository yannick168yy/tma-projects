---
description: 用户要「方案/选项」时先进入规划模式，列选项后再实现
alwaysApply: true
---

# 要方案时先规划，勿直接改代码

当用户明确要求**方案、选项、思路、对比、怎么做好**，或类似表述（如「有什么方案」「几种做法」「你建议怎么」）且**未**明确要求立即实现时：

1. **先进入 Plan 模式**（`SwitchMode` → `plan`），在规划模式下讨论。
2. **只输出方案**：列出 2–4 个可选方向，说明视觉效果/实现成本/适用场景与取舍；可用表格或简短示意。
3. **等用户选定**（或明确「按方案 X 做」「直接实现」）后，再切回 Agent 模式写代码、提交、部署。
4. **禁止**在用户仅要方案时：直接改文件、commit、push、部署。

## 触发示例

- 「Toast 背景看着别扭，你有什么方案？」→ Plan：列 A/B/C，不先改 CSS。
- 「重构这块 API 怎么设计比较好？」→ Plan：架构选项 + 利弊。
- 「按方案 B 做」/「就按你说的金色那条实现」→ Agent：开始编码。

## 例外（可直接实现）

- 用户同时说「实现」「改一下」「按 X 做」。
- 明显是 bug 修复且方案唯一（如拼写错误、缺 import），无需多方案讨论。
