---
description: 阿里云测试环境默认用 Fast 部署（deploy-fast.sh），避免全量 podman build
alwaysApply: true
---

# 部署：默认 Fast 模式

**纯代码改动（无新 npm 依赖、无 Dockerfile / 环境变量变更）时，一律用 Fast 部署，约 20–40 秒。不要用 `deploy-web-tma.sh` / `podman-prod-minimal.sh` 全量重建。**

## 命令模板

```bash
cd /Users/yannicky/tma-projects && \
DEPLOY_HOST=root@47.84.34.139 \
DEPLOY_DIR=/root/workspace/tma-projects \
SSH_IDENTITY_FILE=/Users/yannicky/TMA_FILES/亚马逊云-阿里云/aliyun.pem \
SSH_OPTS="-o StrictHostKeyChecking=no" \
bash deploy/single-node/deploy-fast.sh <目标>
```

**目标：** `web-tma` | `bff-node` | `core-node` | `all`（默认 `all`）

| 目标 | 流程 |
|------|------|
| `web-tma` | 本地 `npm run build` → rsync `dist/` → **无需 restart**（宿主机目录挂载到 nginx） |
| `bff-node` | 本地 `npm run build` → rsync `dist/` → `podman restart tma-bff-node` |
| `core-node` | 本地 `npm run build` → rsync `dist/` → `podman restart tma-core-node` |

## 何时才用完整部署

仅当满足其一：`package.json` 新增依赖、Dockerfile 变更、首次上架、MySQL/迁移/compose 变更、需重建镜像时：

```bash
bash deploy/single-node/deploy-web-tma.sh
```

完整部署后可在服务器执行 `podman image prune -f`，避免悬空镜像占盘。

## 改完代码后的 Agent 流程

与 `git-and-deploy-on-request.mdc` 一致：commit 后按改动范围选 `deploy-fast.sh` 目标；**禁止**为日常前后端改动默认跑全量 podman build。
