---
description: 参考 Karpathy 的 LLM 编程规范：先想清楚、保持简单、改动要外科手术式、按可验证目标推进
alwaysApply: true
---

# Karpathy 风格：减少 LLM 常见编码失误

目标：**降低过度实现/乱改/无验证就结束**等常见问题；与本项目既有规则（中文、自动提交/部署等）并行生效。

## 1) 写代码前先想清楚

- **先明确目标与验收标准**：把「做一下」翻译成可验证结果（能跑、能复现、能对比）。
- **显式写出假设**：信息不全时，写出你采用的假设与理由，然后继续执行；后续若证伪再调整。
- **有多种解释就把分歧说出来**：不要默默选一个导致跑偏。
- **有明显更简单的做法要指出**：能 50 行解决就别写 200 行。

> 备注：我们这里倾向“不断档推进”。除非缺信息会导致不可逆/高风险（例如生产数据、付款逻辑），否则优先**写清假设并继续**，而不是反复追问。

## 2) 简单优先（最小可用）

- **不做用户没要的功能**（尤其是“顺手加个配置/通用化/扩展点”）。
- **不提前抽象**：一次性代码不要抽公共层。
- **不过度兜底**：不为“不可能发生”的场景加大量错误处理。
- **发现写复杂了就重写简化**：让资深工程师一眼看懂、可维护。

## 3) 外科手术式修改

- **只改必须改的行**：每一行改动都能追溯到用户诉求。
- **不顺手重构/格式化邻近代码**：除非和本次需求直接相关。
- **匹配项目既有风格**：即便你有更喜欢的写法也先保持一致。
- **只清理“你造成的”无用代码**：例如你改完导致的 unused import/变量；别顺手删历史遗留（可提示用户但不动）。

## 4) 目标驱动 + 闭环验证

- **把任务拆成可验证步骤**：每一步都有“怎么确认已完成”的检查点（build、typecheck、接口返回、页面交互）。
- **能写测试就写测试**：bug 修复优先让 bug 可复现，再让它消失。
- **没验证不算完成**：至少做到本项目要求的 build/typecheck/部署闭环（以现有项目规则为准）。

